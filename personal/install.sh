# Custom actions to take on initial install of dotfiles.
# This runs after default install actions, so you can overwrite changes it makes if you want.git clone https://github.com/powerline/fonts.git --depth=1
npm i yalc -g
